<UserControl x:Class="SterlingExample.Views.DebugView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Converter="clr-namespace:SterlingExample.Converter" mc:Ignorable="d"
    d:DesignHeight="200" d:DesignWidth="400">
    <UserControl.Resources>
        <Converter:LogLevelConverter x:Key="LogConverter"/>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <ListBox Width="190"                  
                 ItemsSource="{Binding Messages}" HorizontalAlignment="Stretch" Margin="5">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="160" HorizontalAlignment="Center" Background="{Binding Severity,Converter={StaticResource LogConverter}}">
                    <TextBlock Margin="5"
                               Foreground="Black"
                               TextWrapping="Wrap" 
                               Text="{Binding Message}"                                
                               ToolTipService.ToolTip="{Binding ExceptionInfo}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
